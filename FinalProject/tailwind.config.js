/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./*.html"], // Scans all HTML files in the root directory
    theme: {
      extend: {},
    },
    plugins: [],
  }